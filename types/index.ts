export type CropArea = {
  x: number;
  y: number;
  width: number;
  height: number;
};

export type TimeRange = {
  start: number;
  end: number;
};

export { Status } from './status';
export { Stage } from './ui';
